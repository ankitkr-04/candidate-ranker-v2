# Precompute stage only (GPU + network). Install on top of the base requirements:
#   pip install -r requirements.txt -r requirements-gpu.txt
# These wheels assume a CUDA GPU and are not installable on a CPU-only machine.
vllm>=0.6
transformers>=4.44
huggingface_hub[hf_transfer]>=0.24
